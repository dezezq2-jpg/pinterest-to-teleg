aiogram>=3.10
beautifulsoup4
apscheduler
python-dotenv
flask
gunicorn
httpx          # ← добавили
requests       # ← оставляем, он ещё используется в keep‑alive (можно удалить, если захотите полностью async)
